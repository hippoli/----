//进入xx文件夹
    cd xxx
//显示文件夹内容
    ls/ll
//编译
    gcc [xxx.c] -o [xxx]
//运行
    ./xxx 
//打印当前目录的绝对路径名
    pwd
//清理终端
    clear 或 ctrl + l(字母l)

路径说明：
    绝对路径：以/开头的路径 如：/mnt/hgfs/
    相对路径： 以.开头的路径， 如：./code
    绝对路径中的第一个/，称为根目录，后面的/称为目录分隔符
    相对路径中的.代表当前目录， ..代表上一层目录

// 建立目录
    mkdir
    如: mkdir /mnt/usb

//查找文件
    find [path] [oprations] [expression]
    参数:
        -name:支持通配符*和?;
        -atime n:在过去n天读取过的文件
        -ctime n:在过去n天修改过的文件
        