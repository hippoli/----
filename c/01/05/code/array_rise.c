//找数组上的最大上升序列
#include <stdio.h>
#include <stdlib.h>
#include <time.h>
int main()
{
    srand(time(NULL));
    int array_rise[20] = {0};
    int array_test[20] = {0};
    int max_num = 0;
    for (int i = 0; i < 20; i++)
    {
        array_rise[i] = rand()%100;
    }
    for (int i = 0; i < 20; i++)
    {
        for (int j = 0; j < 20; j++)
        {
            
        }
        
    }
    
    return 0;
}